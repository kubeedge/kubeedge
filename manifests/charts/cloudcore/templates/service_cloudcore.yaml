{{- if and (.Values.cloudCore.service.enable) }}
apiVersion: v1
kind: Service
metadata:
  {{- with .Values.cloudCore.service.annotations }}
  annotations: {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.cloudCore.labels }}
  labels: {{- toYaml . | nindent 4 }}
  {{- end }}
  name: cloudcore
spec:
  {{- if and (eq .Values.cloudCore.service.type "NodePort") ( not .Values.cloudCore.hostNetWork) }}
  type: {{ .Values.cloudCore.service.type }}
  {{- else }}
  type: ClusterIP
  {{- end }}
  ports:
  - port: {{ .Values.cloudCore.modules.cloudHub.websocket.port }}
    targetPort: {{ .Values.cloudCore.modules.cloudHub.websocket.port }}
    {{- if and (eq .Values.cloudCore.service.type "NodePort") ( not .Values.cloudCore.hostNetWork) }}
    nodePort: {{ .Values.cloudCore.service.cloudhubNodePort }}
    {{- end }}
    name: cloudhub
  - port: {{ .Values.cloudCore.modules.cloudHub.quic.port }}
    targetPort: {{ .Values.cloudCore.modules.cloudHub.quic.port }}
    protocol: UDP
    {{- if and (eq .Values.cloudCore.service.type "NodePort") ( not .Values.cloudCore.hostNetWork) }}
    nodePort: {{ .Values.cloudCore.service.cloudhubQuicNodePort }}
    {{- end }}
    name: cloudhub-quic
  - port: {{ .Values.cloudCore.modules.cloudHub.https.port }}
    targetPort: {{ .Values.cloudCore.modules.cloudHub.https.port }}
    {{- if and (eq .Values.cloudCore.service.type "NodePort") ( not .Values.cloudCore.hostNetWork) }}
    nodePort: {{ .Values.cloudCore.service.cloudhubHttpsNodePort }}
    {{- end }}
    name: cloudhub-https
  - port: 10003
    targetPort: 10003
    {{- if and (eq .Values.cloudCore.service.type "NodePort") ( not .Values.cloudCore.hostNetWork) }}
    nodePort: {{ .Values.cloudCore.service.cloudstreamNodePort }}
    {{- end }}
    name: cloudstream
  - port: 10004
    targetPort: 10004
    {{- if and (eq .Values.cloudCore.service.type "NodePort") ( not .Values.cloudCore.hostNetWork) }}
    nodePort: {{ .Values.cloudCore.service.tunnelNodePort }}
    {{- end }}
    name: tunnelport
  selector:
  {{- with .Values.cloudCore.labels }}
  {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
